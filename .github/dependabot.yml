version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      # Weekly checks keep pinned workflow actions current without excess noise.
      interval: "weekly"
